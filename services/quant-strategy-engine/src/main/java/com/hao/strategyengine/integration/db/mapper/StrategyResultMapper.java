package com.hao.strategyengine.integration.db.mapper;

import org.apache.ibatis.annotations.Insert;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface StrategyResultMapper {
//    @Insert("INSERT INTO strategy_result (combo_key, payload_json, created_at) VALUES (#{comboKey}, #{payloadJson}, #{createdAt})")
//    void insert(StrategyResultEntity e);
}
